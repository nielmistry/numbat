[workspace]
resolver = "2"

members = [
    "numbat",
    "numbat-exchange-rates",
    "numbat-pubchem",
    "numbat-cli",
]

exclude = [
    "numbat-wasm"
]

[profile.release]
lto = true
strip = true
codegen-units = 1
